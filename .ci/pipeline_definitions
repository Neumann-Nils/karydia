karydia:
  jobs:
    head-update:
      traits:
        version: ~
        publish:
          dockerimages:
            first_image: # logical image name
              dockerfile: 'Dockerfile'
              tag_as_latest: False
      steps:
        test: ~
